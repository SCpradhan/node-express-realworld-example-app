# Repository Metadata Knowledge Base

## `.vscode`
| File Path | Core Purpose | Exposed Functions |
|-----------|--------------|-------------------|
| `.vscode/extensions.json` | This is a Visual Studio Code workspace configuration file that specifies recommended extensions for developers working on this Node.js Express RealWorld example application. It suggests four extensions: Angular Console, Prettier for code formatting, Jest test runner, and ESLint for JavaScript linting. |  |

## `/`
| File Path | Core Purpose | Exposed Functions |
|-----------|--------------|-------------------|
| `.eslintrc.json` | Defines ESLint configuration for a monorepo managed by Nx, establishing linting rules and overrides for TypeScript, JavaScript, and test files. Configures the root-level linting behavior with Nx-specific plugins and extends standard Nx presets for different file types. |  |
| `jest.config.ts` | Configures Jest testing framework settings for the API module of a Node.js Express application. Defines test environment, file transformation rules using ts-jest for TypeScript files, test file matching patterns, and coverage output directory. |  |
| `jest.preset.js` | Serves as the Jest testing configuration preset file for the node-express-realworld-example-app project. This file imports and re-exports the default Nx Jest preset configuration, establishing the baseline testing framework settings for the entire monorepo workspace. |  |
| `nx.json` | Defines the Nx workspace configuration for a Node.js Express RealWorld example application. Specifies build system defaults, caching strategies, task dependencies, and input file patterns for build, lint, and test targets across the monorepo. |  |
| `package-lock.json` | Failed after 3 attempts. |  |
| `package.json` | Defines the Node.js project configuration and dependency manifest for a RealWorld API implementation built with Express. Configures Nx monorepo tooling, Prisma ORM with seeding capabilities, and establishes build, test, and development scripts for the application. |  |
| `project.json` | Defines the Nx workspace project configuration for the 'api' application in a Node.js/Express RealWorld example app. Specifies build targets using esbuild, serve configurations, linting with ESLint, testing with Jest, and Docker containerization commands for both development and production environments. |  |

## `e2e`
| File Path | Core Purpose | Exposed Functions |
|-----------|--------------|-------------------|
| `e2e/.eslintrc.json` | Defines ESLint configuration for the e2e testing directory in an Nx monorepo workspace. Specifies linting rules and plugin extensions for TypeScript and JavaScript files, ensuring code quality standards are enforced across end-to-end test files. |  |
| `e2e/jest.config.ts` | Configures Jest testing framework for end-to-end (e2e) tests in a Node.js/TypeScript environment. Defines test environment settings, TypeScript transformation rules, global setup/teardown hooks, and coverage output directory for the e2e test suite. |  |
| `e2e/project.json` | Defines the Nx workspace project configuration for the end-to-end testing suite. Specifies build targets for running Jest-based e2e tests and ESLint validation, with an implicit dependency on the 'api' project to ensure proper test execution order. |  |
| `e2e/tsconfig.json` | TypeScript configuration file for the end-to-end (e2e) testing directory. Extends the root tsconfig.json and references the e2e spec configuration file while enabling ES module interoperability for test execution. |  |
| `e2e/tsconfig.spec.json` | Defines TypeScript compiler configuration specifically for end-to-end test specifications in the node-express-realworld-example-app project. Extends the base tsconfig.json and configures Jest test environment with CommonJS module output targeting the dist/out-tsc directory. |  |

## `e2e/src/server`
| File Path | Core Purpose | Exposed Functions |
|-----------|--------------|-------------------|
| `e2e/src/server/server.spec.ts` | This is an end-to-end test specification file that validates the server's root endpoint behavior. It contains a single test suite that verifies the GET / route returns a 200 status code and a JSON response with a 'Hello API' message. |  |

## `e2e/src/support`
| File Path | Core Purpose | Exposed Functions |
|-----------|--------------|-------------------|
| `e2e/src/support/global-setup.ts` | Serves as the global setup configuration file for end-to-end testing infrastructure. Initializes test environment prerequisites such as databases or docker-compose services before test execution begins, and passes teardown messages to the global teardown phase via globalThis. | <b>`module.exports = async function ()`</b>: Asynchronous setup function that initializes required services for the test suite and configures global teardown messaging through globalThis. |
| `e2e/src/support/global-teardown.ts` | Serves as the global teardown hook for end-to-end test execution. This module exports an async function that runs after all tests complete, providing a centralized location for cleanup logic such as stopping services or docker-compose containers, and logs a teardown message from the global context. | <b>`module.exports = async function ()`</b>: Executes global cleanup operations after all e2e tests have completed and logs the teardown message stored in globalThis during test setup. |
| `e2e/src/support/test-setup.ts` | Configures the global axios HTTP client for end-to-end test execution. Sets the base URL for API requests by reading host and port from environment variables or using localhost:3000 as defaults. | <b>`module.exports = async function ()`</b>: Asynchronous setup function that configures axios defaults with the appropriate base URL for the test environment. |

## `src/app/models`
| File Path | Core Purpose | Exposed Functions |
|-----------|--------------|-------------------|
| `src/app/models/http-exception.model.ts` | Defines a custom HTTP exception model that extends the native Error class to encapsulate HTTP error responses with specific error codes. This class serves as a standardized error structure for the application's HTTP layer, allowing consistent error handling and propagation throughout the Express-based RealWorld API. | <b>`constructor(errorCode: number, public readonly message: string | any)`</b>: Initializes a new HttpException instance with a specific HTTP error code and error message, setting up the error object for propagation through the application's error handling middleware. |

## `src/app/routes`
| File Path | Core Purpose | Exposed Functions |
|-----------|--------------|-------------------|
| `src/app/routes/routes.ts` | Serves as the main API routing configuration file for a Node.js Express application implementing the RealWorld example app. It aggregates and mounts all feature-specific route controllers (tags, articles, profiles, authentication) under a unified '/api' prefix to establish the application's REST API structure. |  |

## `src/app/routes/article`
| File Path | Core Purpose | Exposed Functions |
|-----------|--------------|-------------------|
| `src/app/routes/article/article.controller.ts` | Defines the Express router controller for article-related HTTP endpoints in a RealWorld API implementation. Orchestrates RESTful operations for articles, comments, and favorites by delegating to service layer functions and handling authentication middleware for protected routes. | <b>`router.get('/articles', auth.optional, async (req: Request, res: Response, next: NextFunction))`</b>: Retrieves a paginated list of articles with optional filtering by tag, author, and favorited status. Authentication is optional to support both authenticated and anonymous users.<br/><b>`router.get('/articles/feed', auth.required, async (req: Request, res: Response, next: NextFunction))`</b>: Returns a personalized feed of articles for authenticated users based on their followed authors. Requires authentication and supports pagination via offset and limit query parameters.<br/><b>`router.post('/articles', auth.required, async (req: Request, res: Response, next: NextFunction))`</b>: Creates a new article with title, description, body, and optional tag list. Requires authentication and returns the created article with 201 status.<br/><b>`router.get('/articles/:slug', auth.optional, async (req: Request, res: Response, next: NextFunction))`</b>: Fetches a single article by its unique slug identifier. Authentication is optional to allow public article viewing.<br/><b>`router.put('/articles/:slug', auth.required, async (req: Request, res: Response, next: NextFunction))`</b>: Updates an existing article identified by slug with new title, description, or body content. Requires authentication to ensure only authorized users can modify articles.<br/><b>`router.delete('/articles/:slug', auth.required, async (req: Request, res: Response, next: NextFunction))`</b>: Deletes an article identified by its slug. Requires authentication and returns 204 No Content status on successful deletion.<br/><b>`router.get('/articles/:slug/comments', auth.optional, async (req: Request, res: Response, next: NextFunction))`</b>: Retrieves all comments associated with a specific article identified by slug. Authentication is optional to allow public comment viewing.<br/><b>`router.post('/articles/:slug/comments', auth.required, async (req: Request, res: Response, next: NextFunction))`</b>: Adds a new comment to an article identified by slug. Requires authentication and returns the created comment object.<br/><b>`router.delete('/articles/:slug/comments/:id', auth.required, async (req: Request, res: Response, next: NextFunction))`</b>: Deletes a specific comment identified by its numeric ID from an article. Requires authentication to ensure only authorized users can remove comments.<br/><b>`router.post('/articles/:slug/favorite', auth.required, async (req: Request, res: Response, next: NextFunction))`</b>: Marks an article as favorited by the authenticated user. Requires authentication and returns the updated article with favorite status.<br/><b>`router.delete('/articles/:slug/favorite', auth.required, async (req: Request, res: Response, next: NextFunction))`</b>: Removes the favorite status from an article for the authenticated user. Requires authentication and returns the updated article. |
| `src/app/routes/article/article.mapper.ts` | Transforms raw article data objects into a standardized article response format for the RealWorld API. Maps article properties including slug, title, description, body, tag list, timestamps, favorited status, favorites count, and author information using the author mapper. | <b>`const articleMapper = (article: any, id?: number) => ({...})`</b>: Accepts an article object and optional user ID, then returns a formatted article object with computed favorited status and mapped author data. |
| `src/app/routes/article/article.model.ts` | Defines the TypeScript interface for an Article entity in a RealWorld example application. This interface specifies the data structure for articles, including metadata fields like id, title, slug, description, associated comments, and favorited status. |  |
| `src/app/routes/article/article.service.ts` | Provides comprehensive service layer operations for article management in a RealWorld blogging application. Handles CRUD operations for articles, comments, and favorites, including querying with filters (author, tag, favorited), feed generation, and authorization checks. Integrates with Prisma ORM for database operations and applies business logic such as slug generation, validation, and relationship mapping. | <b>`export const getArticles = async (query: any, id?: number)`</b>: Retrieves a paginated list of articles with optional filtering by author, tag, or favorited user, including counts and mapped article data.<br/><b>`export const getFeed = async (offset: number, limit: number, id: number)`</b>: Fetches a personalized feed of articles from authors that the authenticated user follows, with pagination support.<br/><b>`export const createArticle = async (article: any, id: number)`</b>: Creates a new article with validation, generates a unique slug, connects tags, and returns the mapped article object.<br/><b>`export const getArticle = async (slug: string, id?: number)`</b>: Retrieves a single article by its slug identifier, including all related data (tags, author, favorites), or throws a 404 error if not found.<br/><b>`export const updateArticle = async (article: any, slug: string, id: number)`</b>: Updates an existing article with authorization checks, handles slug regeneration if title changes, manages tag updates, and validates uniqueness constraints.<br/><b>`export const deleteArticle = async (slug: string, id: number)`</b>: Deletes an article by slug after verifying the requesting user is the author, throwing appropriate authorization errors if not.<br/><b>`export const getCommentsByArticle = async (slug: string, id?: number)`</b>: Retrieves all comments for a specific article, filtered by demo users or the authenticated user, with author profile information.<br/><b>`export const addComment = async (body: string, slug: string, id: number)`</b>: Creates a new comment on an article with validation, connects it to the article and author, and returns the formatted comment object.<br/><b>`export const deleteComment = async (id: number, userId: number)`</b>: Deletes a comment by ID after verifying the requesting user is the comment author, with authorization checks.<br/><b>`export const favoriteArticle = async (slugPayload: string, id: number)`</b>: Adds the authenticated user to an article's favorites list and returns the updated article with favorite status and count.<br/><b>`export const unfavoriteArticle = async (slugPayload: string, id: number)`</b>: Removes the authenticated user from an article's favorites list and returns the updated article with favorite status and count. |
| `src/app/routes/article/author.mapper.ts` | Provides a data transformation utility that maps raw author data objects into a standardized author response format for article endpoints. Determines the following status of an author by checking if the requesting user's ID exists in the author's followedBy collection. | <b>`const authorMapper = (author: any, id?: number)`</b>: Transforms an author object into a standardized format containing username, bio, image, and following status based on the optional user ID parameter. |
| `src/app/routes/article/comment.model.ts` | Defines the TypeScript interface for a Comment entity in a RealWorld example application. Specifies the data structure for comments associated with articles, including metadata fields like id, timestamps, body text, and an optional reference to the parent Article. |  |

## `src/app/routes/auth`
| File Path | Core Purpose | Exposed Functions |
|-----------|--------------|-------------------|
| `src/app/routes/auth/auth.controller.ts` | Delayed response - Exceeded 5 minutes. |  |
| `src/app/routes/auth/auth.service.ts` | Provides authentication and user management services for a RealWorld example application. Handles user registration with uniqueness validation, login with password verification, retrieval of current user data, and user profile updates. Integrates with Prisma ORM for database operations and bcrypt for password hashing, returning JWT tokens for authenticated sessions. | <b>`export const createUser = async (input: RegisterInput): Promise<RegisteredUser>`</b>: Registers a new user by validating input fields, checking email and username uniqueness, hashing the password, creating the user record in the database, and returning the user data with a JWT token.<br/><b>`export const login = async (userPayload: any)`</b>: Authenticates a user by validating credentials, comparing the provided password with the stored hashed password, and returning user data with a JWT token if authentication succeeds.<br/><b>`export const getCurrentUser = async (id: number)`</b>: Retrieves the current user's profile information from the database by user ID and returns it with a fresh JWT token.<br/><b>`export const updateUser = async (userPayload: any, id: number)`</b>: Updates user profile information including email, username, password, image, and bio, hashing the new password if provided, and returns the updated user data with a JWT token. |
| `src/app/routes/auth/auth.ts` | Provides JWT-based authentication middleware for Express routes in a Node.js application. Exports two authentication strategies: required (enforces JWT validation) and optional (allows requests without credentials), both configured to extract tokens from Authorization headers using Bearer or Token schemes. | <b>`const getTokenFromHeaders = (req: express.Request): string | null`</b>: Extracts and returns the JWT token from the Authorization header if it uses Bearer or Token scheme, otherwise returns null. |
| `src/app/routes/auth/register-input.model.ts` | Defines the TypeScript interface for user registration input data in the authentication module. Specifies the required and optional fields needed to register a new user account, including email, username, password, and optional profile information. |  |
| `src/app/routes/auth/registered-user.model.ts` | Defines the TypeScript interface for a RegisteredUser entity in the authentication module. This interface specifies the data structure for a user who has completed registration, including their profile information and authentication token. |  |
| `src/app/routes/auth/token.utils.ts` | Provides JWT token generation utility for user authentication in the RealWorld example application. Creates signed tokens containing user ID with a 60-day expiration period using a configurable secret key from environment variables. | <b>`const generateToken = (id: number): string`</b>: Generates and returns a JWT token for a given user ID, signing it with the application secret and setting a 60-day expiration. |
| `src/app/routes/auth/user-request.d.ts` | Provides TypeScript type augmentation for Express.js Request objects to include optional authentication metadata. Extends the Express namespace to add an auth property containing user identification information for authenticated requests. |  |
| `src/app/routes/auth/user.model.ts` | Defines the User entity interface for the RealWorld application, representing the core user data model with authentication credentials, profile information, and relationships to articles, comments, and other users. This interface serves as the central type definition for user-related operations throughout the authentication and social features of the application. |  |

## `src/app/routes/profile`
| File Path | Core Purpose | Exposed Functions |
|-----------|--------------|-------------------|
| `src/app/routes/profile/profile.controller.ts` | Defines Express router endpoints for user profile operations in a RealWorld application. Exposes three HTTP routes for retrieving user profiles, following users, and unfollowing users, delegating business logic to the profile service layer. | <b>`router.get('/profiles/:username', auth.optional, async (req: Request, res: Response, next: NextFunction))`</b>: Retrieves and returns a user profile by username with optional authentication, allowing both authenticated and unauthenticated access.<br/><b>`router.post('/profiles/:username/follow', auth.required, async (req: Request, res: Response, next: NextFunction))`</b>: Allows an authenticated user to follow another user by username and returns the updated profile.<br/><b>`router.delete('/profiles/:username/follow', auth.required, async (req: Request, res: Response, next: NextFunction))`</b>: Allows an authenticated user to unfollow another user by username and returns the updated profile. |
| `src/app/routes/profile/profile.model.ts` | Defines the TypeScript interface for a user Profile entity in the RealWorld application. This interface specifies the structure for profile data including username, bio, image URL, and following status used across profile-related features. |  |
| `src/app/routes/profile/profile.service.ts` | Provides profile management services for a social networking application, handling user profile retrieval, follow/unfollow operations. Interacts with the database through Prisma ORM to manage user relationships and returns mapped profile data. | <b>`export const getProfile = async (usernamePayload: string, id?: number)`</b>: Retrieves a user profile by username from the database, including follower relationships, and returns a mapped profile object. Throws a 404 exception if the profile is not found.<br/><b>`export const followUser = async (usernamePayload: string, id: number)`</b>: Establishes a follow relationship between the authenticated user (by id) and the target user (by username), updating the database and returning the mapped profile.<br/><b>`export const unfollowUser = async (usernamePayload: string, id: number)`</b>: Removes a follow relationship between the authenticated user (by id) and the target user (by username), updating the database and returning the mapped profile. |
| `src/app/routes/profile/profile.utils.ts` | Provides a utility function for transforming User domain objects into Profile view models for the profile feature. Maps user data including username, bio, image, and following status based on the requesting user's ID. | <b>`const profileMapper = (user: any, id: number | undefined): Profile`</b>: Transforms a User object into a Profile object, determining the following status by checking if the requesting user (identified by id) is in the target user's followedBy list. |

## `src/app/routes/tag`
| File Path | Core Purpose | Exposed Functions |
|-----------|--------------|-------------------|
| `src/app/routes/tag/tag.controller.ts` | Defines the Express router controller for the tags endpoint in a RealWorld API implementation. Exposes a single GET route that retrieves the top 10 popular tags, with optional authentication support. | <b>`router.get('/tags', auth.optional, async (req: Request, res: Response, next: NextFunction) => {...})`</b>: Handles GET requests to /api/tags endpoint to retrieve a list of popular tag names, optionally using authenticated user context. |
| `src/app/routes/tag/tag.model.ts` | Defines the TypeScript interface for a Tag entity in the RealWorld example application. This interface establishes the data contract for tag objects, specifying that each tag must have a name property of type string. |  |
| `src/app/routes/tag/tag.service.ts` | Provides tag retrieval functionality for the RealWorld application by querying tags associated with articles from demo or specific authors. Returns the top 10 most popular tags ordered by article count. | <b>`const getTags = async (id?: number): Promise<string[]>`</b>: Retrieves and returns an array of tag names from the database, optionally filtered by author ID, ordered by article count descending and limited to 10 results. |

